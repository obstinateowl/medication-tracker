{
  "name": "medication-tracker",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && npm run build --prefix server",
    "start": "npm run start --prefix server",
    "start:prod": "SERVE_STATIC=true NODE_ENV=production npm run start --prefix server",
    "check-db": "npm run check-db --prefix server",
    "install:prod": "bash scripts/install.sh",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down",
    "docker:logs": "docker compose logs -f",
    "package:ha": "bash scripts/package-for-ha.sh",
    "verify:ha": "bash scripts/verify-ha-package.sh",
    "deploy:ha": "bash scripts/deploy-to-ha.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
